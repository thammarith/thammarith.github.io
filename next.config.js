/** @type {import('next').NextConfig} */
const nextConfig = {
	// i18n: {
	// 	locales: ['en', 'th'],
	// 	defaultLocale: 'en',
	// },
	images: {
        domains: ['thammarith.dev'],
    },
	reactStrictMode: true,
};

module.exports = nextConfig;
